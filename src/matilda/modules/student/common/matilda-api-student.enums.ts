export enum StudentStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  DELETED = 'deleted',
}

export enum StudentType {
  STUDENT = 'student',
}

export enum DocumentType {
  CURP = 'CURP',
  NONE = 'None',
}
